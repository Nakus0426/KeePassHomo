# KeePassHomo 统一开发计划

> 基线日期：2026-08-20
>
> 目标版本：首个可发布版本（P0）
>
> 目标平台：HarmonyOS API 26，Phone 与 Tablet
>
> 产品定位：面向个人用户、本地优先、不提供产品账号体系的 KeePass 第三方客户端

## 1. 文档用途

本文是 KeePassHomo 唯一的产品范围、设计约束、开发顺序和验收基线。需求、架构、页面、状态与进度均在本文维护，不再拆分独立设计清单。

### 1.1 状态约定

- `[ ]`：未开始、未完成或尚未通过用户手动验收。
- `[x]`：实现、静态检查、构建验证和用户手动验收均完成。
- 部分完成不得标记为完成；受阻事项保持未勾选，并写入“风险与决策记录”。

### 1.2 优先级

- `[P0]`：首版必须完成，缺失会导致核心流程、安全边界或产品信息架构不完整。
- `[P1]`：首版只做能力验证，验证通过后另行决定是否实现。
- `[P2]`：后续版本候选，不进入 P0 主流程。

## 2. 产品边界

### 2.1 产品原则

- [ ] `[P0]` 用户无需注册、登录产品账号或购买订阅即可使用。
- [ ] `[P0]` KDBX 文件是唯一数据真源，不建立不可导出的私有密码库格式。
- [ ] `[P0]` 默认离线可用；仅 WebDAV 同步和用户主动开启的泄露检测需要联网。
- [ ] `[P0]` 高风险操作必须明确反馈，不允许静默覆盖、上传、删除或降低安全级别。
- [ ] `[P0]` 敏感值默认遮挡；截图、最近任务、剪贴板、日志、路由和后台切换均按安全优先处理。
- [ ] `[P0]` Phone 与 Tablet 共享相同信息架构；Tablet 只利用额外空间提升效率，不增加独有功能。
- [ ] `[P0]` 可参考成熟密码管理器的功能完整性和操作效率，不复制其品牌、配色或页面构图。

### 2.2 明确不做

- [ ] `[P1]` 系统级自动填充只做能力验证，不承诺 P0 接管系统密码保险箱。
- [ ] `[P2]` Passkey 保存与填充。
- [ ] `[P2]` Dropbox、OneDrive、Google Drive、SFTP 等额外同步源。
- [ ] `[P2]` KeePass 1.x `.kdb` 文件导入。
- [ ] `[P2]` 家庭共享、团队空间、成员权限和审计后台。
- [ ] `[P2]` 产品账号、订阅和云端托管密码库。

## 3. 架构与执行规则

### 3.1 开发顺序

```text
技术闸门与本地最小闭环（已完成）
  -> 架构与全局能力底座
  -> 保险库模块
  -> 安全中心模块
  -> 生成器模块
  -> 设置模块（含 WebDAV 与同步）
  -> Tablet、无障碍、状态覆盖与发布收敛
  -> P1 自动填充能力验证
```

执行约束：

- 架构和跨模块能力必须先稳定，再开发依赖它们的页面。
- 四个底部导航模块严格按“保险库 → 安全中心 → 生成器 → 设置”顺序完成；前一模块未通过人工验收，不进入下一模块。
- 同一模块按“领域模型 → ViewModel → 页面 → 异常状态 → 构建 → 人工验收”完成纵向闭环。
- 不为了铺页面同时并行开发多个主模块，不创建无业务行为的视觉占位功能。
- WebDAV 页面归属设置模块；其接口、版本比较和冲突状态机在全局底座阶段先定义。

### 3.2 分层与依赖方向

```text
View（ArkUI）
  -> ViewModel（状态管理 V2）
  -> Domain Service（业务规则）
  -> Repository / Engine（文件、KDBX、网络、系统能力）
```

- View 只负责渲染和用户事件，不直接调用文件、密码学或网络接口。
- ViewModel 管理页面状态和业务编排，不持有不可控的明文敏感副本。
- Domain Service 不依赖 ArkUI 组件。
- Repository 和 Engine 只在系统边界校验外部输入与错误。
- Ability 只处理生命周期、窗口初始化和全局隐私遮罩协调。
- 公共能力放入 `common`；单模块 View、ViewModel 和状态留在所属业务模块。

### 3.3 状态管理

- 新页面、组件和 ViewModel 优先使用状态管理 V2：`@ComponentV2`、`@ObservedV2`、`@Trace`、`@Local`、`@Param`、`@Event`、
  `@Monitor` 和 `@Computed`。
- 应用级共享状态优先使用 `AppStorageV2`；确需持久化的非敏感设置使用 `PersistenceV2`。
- 敏感值、解锁会话和临时候选不得因使用 `PersistenceV2` 而落盘。
- 父向子传递使用 `@Param`，子向父发出意图使用 `@Event`，避免隐式双向修改。
- 复杂领域对象使用 `@ObservedV2 + @Trace` 做属性级观测，避免页面通过复制数组或对象强制刷新。
- 只有确认 API 26 下 V2 无法满足具体场景，并记录官方依据、限制和影响后，才允许局部使用 V1。
- 现有 V1 代码按页面边界迁移，不在一次功能开发中同时保留两套状态真源。

### 3.4 路由与模块边界

- [x] 使用 `Navigation + NavPathStack + NavDestination` 建立真实页面栈。
- [x] 集中维护认证、保险库、条目、安全中心、生成器和设置路由名称。
- [x] 路由参数不传递密码、凭据、TOTP 密钥、完整条目或其他敏感数据。
- [x] Phone 使用单栈导航和四模块悬浮胶囊底栏。
- [ ] 每个主模块拥有独立页面入口、ViewModel 和内部路由，不再集中于单个巨型页面组件。
- [x] 页面文件按一页一文件组织；仅当前页面使用的组件可与页面同文件，跨页面复用的组件再抽取为独立文件，禁止多个页面集中在同一个 `.ets` 文件。
- [ ] 全局锁定后清空业务返回栈，统一进入安全遮罩与解锁流程。
- [ ] Tablet 复用同一路由名称和领域状态，只替换页面容器与布局策略。

### 3.5 写入、同步与安全顺序

1. 用户保存条目后，在本机安全临时文件中生成完整 KDBX。
2. 新文件可重新打开并校验后，安全替换当前本地文件。
3. 本地保存成功立即反馈；WebDAV 上传是后续同步步骤。
4. 上传前比较远端版本标识、修改时间和内容摘要。
5. 远端未变化才上传；本地与远端均变化时进入冲突。
6. 冲突解决前不覆盖任何版本；替换前备份将被替换的版本。

## 4. 全局信息架构与设计系统

### 4.1 主导航

| 入口   | Phone     | Tablet    | 默认落点      |
|------|-----------|-----------|-----------|
| 保险库  | 底部悬浮胶囊    | 左侧悬浮导航栏   | 最近分组或全部条目 |
| 安全中心 | 底部悬浮胶囊    | 左侧悬浮导航栏   | 安全概览      |
| 生成器  | 底部悬浮胶囊    | 左侧悬浮导航栏   | 随机密码      |
| 设置   | 底部悬浮胶囊    | 左侧悬浮导航栏   | 设置首页      |
| 搜索   | 保险库顶部搜索胶囊 | 条目列表顶部搜索栏 | 全库搜索      |
| 锁定   | 顶部锁定按钮    | 侧栏底部锁定控件  | 解锁页       |

- 引导、数据源选择和解锁阶段隐藏主导航。
- 切换底部模块必须执行真实路由跳转，不以条件分支替换主页面组件。
- 主模块切换保留必要的非敏感界面状态；锁定时全部清除。

### 4.2 页面容器规则

| 容器   | 适用内容                   | 典型场景                          |
|------|------------------------|-------------------------------|
| 独立页面 | 高频主任务、长列表、长表单、稳定返回节点   | 保险库、条目详情/编辑、安全中心、生成器、同步中心、设置  |
| 半模态  | 从当前上下文发起、完成后返回原位置的中等任务 | 模板选择、生物认证、旧库升级、TOTP、移动、冲突、再认证 |
| 临时浮层 | 少量选项、筛选、排序和更多操作        | 条目排序、筛选、标签、同步状态菜单             |
| 确认弹窗 | 不可逆操作和阻断错误             | 永久删除、清空回收站、放弃修改、证书错误          |

- 同一时刻只显示一个半模态；简单二级选项使用面板内菜单。
- 半模态关闭后恢复滚动位置、筛选、选中项和键盘焦点。
- 未保存内容、高风险确认或进行中任务不得因点击遮罩直接丢失。
- 无法继续当前任务的错误才使用完整错误页；可原地恢复的错误使用半模态或行内反馈。

### 4.3 沉浸光感

| 场景               | 材质档位          | 约束                |
|------------------|---------------|-------------------|
| 顶部标题栏、搜索栏        | `ULTRA_THIN`  | 内容延伸至其下，滚动时平滑增加模糊 |
| 底部导航、操作坞、MiniBar | `THIN`        | 与安全区保持悬浮间距        |
| 临时菜单、筛选面板        | `THICK`       | 关闭后不保留光效          |
| 半模态、操作面板、阻断弹层    | `ULTRA_THICK` | 稳定对比度和明确遮罩层级      |
| 列表、表单、密码字段、风险正文  | 无常驻强材质        | 使用稳定实色内容面         |

- [ ] `[P0]` 默认使用系统 `ADAPTIVE` 材质级别，尊重系统强度和设备性能。
- [ ] `[P0]` 除明确隐藏标题栏的页面外，标题栏默认使用 HDS 内置标题栏、系统 `ADAPTIVE` 沉浸光感材质和
  `IMMERSIVE_GRADIENT_BLUR` 动态渐变模糊；绑定实际滚动容器，并确保默认位置的内容避让标题栏和底部导航区域，滚动后才允许内容进入标题栏背后参与模糊效果。
- [ ] `[P0]` 同屏常驻悬浮层不超过顶部栏、底部栏和一个上下文层。
- [ ] `[P0]` 不使用玻璃卡片嵌套、营销式渐变、装饰色球或无功能光斑。
- [ ] `[P0]` 按压、滚动和材质变化遵循减少动态效果设置，不使用夸张弹跳。
- [ ] `[P0]` 高级材质不可用、节能、低性能或减少透明度时，只降低材质，不改变布局与功能。

### 4.4 色彩、尺寸与无障碍

- [x] 颜色、间距、圆角、字号、标准控件和系统组件尺寸优先复用 API 26 SDK `sysResource.js` 中语义一致的
  `sys.color.*` 与 `sys.float.*`；新增应用 Token 前必须查官方 API 与 SDK 定义，禁止仅因数值相同进行替换。
- [x] 所有 UI 颜色默认使用 `sys.color.*`，品牌色使用 `sys.color.brand`，成功、提醒、错误状态分别使用
  `sys.color.confirm`、`sys.color.alert`、`sys.color.warning`；除非用户明确指定颜色，否则不新增自定义应用颜色。
- [x] 仅在配置 schema 无法引用系统颜色（例如启动窗口配置）或用户明确指定颜色时维护应用颜色资源；业务布局约束等产品专属尺寸
  Token 可保留，并删除已由系统资源替代或无引用的重复 Token。
- [x] 浅色与深色通过 `resources/base`、`resources/dark` 同名资源覆盖。
- [x] 品牌色和业务状态色跟随 HarmonyOS 系统颜色角色及深浅色策略。
- [x] 字符串资源按页面/功能拆分为多个 `string_<scope>.json`；跨页面文案集中到 `string_common.json`，保持资源 key 全局唯一。
- [ ] 背景使用不透明的系统背景 Token；成功、提醒、警告使用独立语义色。
- [ ] 风险同时使用 Symbol、文字和严重度，不只依赖颜色。
- [ ] 常见操作使用 HarmonyOS 系统 Symbol；图标按钮提供无障碍名称和触摸提示。
- [ ] 固定导航、图标按钮、计数器和 TOTP 倒计时使用稳定尺寸。
- [ ] 支持系统字体缩放、高对比度、减少透明度和减少动态效果。
- [ ] 长库名、网址、文件名和错误文本允许换行或省略，并提供完整查看入口。

## 5. 已完成阶段

### 阶段 0：技术闸门与工程基线 `[x]`

- [x] 采用 `kdbxweb@2.3.0`，验证 KDBX 4.x 读写、KDBX 3.1 识别和升级路径。
- [x] 验证主密码、密钥文件、受保护字段、附件、历史、回收站和错误分类。
- [x] 验证本地文件授权、安全替换、用户认证、隐私窗口、剪贴板、WebDAV、相机和 `otpauth` 能力。
- [x] 定义 `VaultEngine`、`VaultRepository`、`VaultSession`、`SyncService` 和 `SecurityAuditService` 边界。
- [x] 关闭真实密码数据的系统备份和结构化数据云同步。
- [x] 建立日志脱敏、资源 Token、正式路由和 API 26 基线。

退出条件已满足：真实 KDBX 可完成打开、解锁、读取、修改、安全写回和重新解锁验证，失败不破坏原文件。

### 阶段 1：本地保险库最小闭环 `[x]`

- [x] A01 启动安全遮罩。
- [x] A02 首次使用与数据源选择。
- [x] A03 创建本地保险库。
- [x] A04 打开、识别、只读处理和重新定位本地 KDBX。
- [x] A06 解锁、错误凭据反馈、防重复提交和最近保险库切换。
- [x] B01 最小保险库首页与空库状态。
- [x] B04 最小登录条目详情和敏感值遮挡。
- [x] B05 登录模板入口。
- [x] B06 登录条目新增、编辑、分组选择、未保存确认和保存。
- [x] C05 随机密码生成核心能力接入密码字段。
- [x] 锁定、重新解锁、10 秒显示、30 秒剪贴板条件清除和 5 分钟自动锁定。

退出条件已满足：创建或打开本地库后，可解锁、新建登录条目、生成密码、保存、锁定并再次解锁读取；阶段 1 已完成人工验收。

## 6. 阶段 2：架构与全局能力底座

目标：清除后续四模块都会依赖的架构债务，建立唯一状态源、模块边界和全局安全行为。

### 6.1 状态与页面拆分

- [x] 将现有主页面从状态管理 V1 迁移为 `@ComponentV2` 页面和 `@ObservedV2` ViewModel。
- [x] 建立应用会话、路由、保险库和生成器的 V2 状态边界；安全中心和设置在产生首个业务状态时建立边界，不创建空 ViewModel。
- [x] 使用 `AppStorageV2` 共享应用会话、锁定状态和当前底栏模块等非敏感全局状态。
- [x] 明确 `PersistenceV2` 只接收非敏感用户设置；当前尚无用户设置，因此不提前创建持久化状态。
- [x] 将巨型入口页面拆为 AppShell、各模块 Page、ViewModel 和可复用业务组件。
- [x] 每个状态只有一个写入所有者；View 通过意图方法或 `@Event` 请求变更，不直接修改其他模块状态。

### 6.2 全局安全与生命周期

- [x] 建立统一 `VaultSessionViewModel`，协调解锁、前后台、手动锁定、自动锁定和敏感状态清理。
- [x] 建立统一再次认证入口和用途契约，服务放大显示、导出、修改凭据、永久删除和诊断导出。
- [x] 锁定时清除搜索词、生成器候选、显示中的秘密、剪贴板任务和业务路由栈。
- [x] 冷启动和恢复前台先显示 A01 安全遮罩，再判断解锁有效期。
- [x] 所有模块共享截图、最近任务和后台隐私保护策略。
- [x] 建立全局脱敏错误模型；错误信息可行动但不包含密码、完整用户名、路径、网址或哈希前缀。

### 6.3 全局服务与错误恢复

- [x] 固化 KDBX 写入事务、备份、重新定位、只读和空间不足处理接口。
- [x] 固化同步版本标识、队列、冲突结果和活动记录数据契约，不提前开发设置页面。
- [x] 建立统一网络、文件、认证、权限和存储错误分类。
- [x] 建立文件不存在、损坏、空间不足、权限失效和认证失效的恢复动作模型。
- [x] 所有新增异步失败显式处理并使用 `hilog` 记录脱敏上下文，不吞错。

### 6.4 全局 UI 基础

- [x] 抽取 Phone AppShell、四模块底栏、顶部栏、页面内容面和错误状态组件；半模态在首个实际业务场景中抽取，不建立一次性空组件。
- [x] 使用系统沉浸材质的设备算力分档能力，并完成深浅色资源和 `fp` 字体基础构建验证。
- [x] 完成现有 `app.color`、`app.float` 审计：系统表面色、文字色、品牌色、业务状态色、间距、圆角、标准字号和 HDS MINI
  标题栏高度改用语义一致的系统资源；仅保留配置边界和产品专属尺寸 Token。
- [ ] 统一需要标题栏的页面为 HDS 内置标题栏，默认启用系统 `ADAPTIVE` 沉浸光感与
  `IMMERSIVE_GRADIENT_BLUR` 动态渐变模糊；使用 `bindToScrollable` 绑定滚动容器，并按标题栏高度和全局安全区配置初始内容避让。
- [x] 建立 Phone 稳定内容宽度和默认安全区规则，为 Tablet 容器替换预留页面边界。
- [x] 清除路由与视觉占位页；未开发模块只保留明确的空状态，不添加推测性功能。

退出条件：入口页面不再集中持有所有业务状态；已有业务状态的模块具有独立 V2
状态边界，未开发模块不创建空状态；锁定、路由、错误和材质行为由全局底座统一提供。开发与构建已完成，等待阶段 2 人工验收。

## 7. 阶段 3：保险库模块

目标：先完整交付底栏“保险库”，覆盖本地 KDBX 的浏览、搜索、条目管理和高级数据。

### 7.1 数据源、认证与格式

- [ ] A07 生物快速解锁：说明只保护本机快速解锁材料；失败、取消和锁定回退主密码。
- [ ] A08 KDBX 3.1 升级：先备份再升级，备份失败禁止继续，失败保留原文件。
- [ ] A03/A04/A06 补齐写入失败、权限拒绝、文件移动、损坏、不支持格式和只读恢复状态。

### 7.2 B01-B03 浏览、分组与搜索

- [ ] B01 展示库名、锁定、同步摘要、分组树、全部条目、收藏、标签、归档和回收站。
- [ ] B01 最近条目只显示图标、标题和非敏感辅助字段；空库提供明确新增入口。
- [ ] B02 支持分组展开/折叠、子分组数量、标题/修改/使用时间排序和类型/标签/风险筛选。
- [ ] B02 支持长按多选；Phone 列表位置和选择状态可恢复。
- [ ] B03 搜索标题、用户名、网址、标签、分组名和非保护字段。
- [ ] B03 默认排除密码、TOTP 密钥和保护字段；搜索历史不持久化，锁库时清除。
- [ ] B03 结果按最佳匹配排序并按条目、分组和标签分区。

### 7.3 B04-B07 详情、编辑与模板

- [ ] B04 展示图标、标题、分组、收藏、风险、常用字段、附件、历史和 TOTP。
- [ ] B04 密码、PIN 和 TOTP 密钥默认遮挡；显示 10 秒或切后台立即隐藏。
- [ ] B04 用户名、密码、网址和 TOTP 提供独立复制；HTTP 地址打开前提示风险。
- [ ] B04 只读库禁用写操作并解释原因。
- [ ] B05 提供登录、银行卡、安全笔记、身份信息、Wi-Fi 和软件许可证六类模板。
- [ ] B05 支持模板搜索和最近使用记录，不改变完整列表顺序。
- [ ] B06 登录编辑支持多用户名、多网址、TOTP、备注、标签、分组和自定义字段。
- [ ] B06 保存成功后返回详情；本地安全写入成功后才进入同步队列。
- [ ] B07 完成其他五类模板；所有敏感字段复用统一显示、复制和再认证策略。

### 7.4 B08-B10 附件、历史与组织

- [ ] B08 附件支持添加、删除、导出；导出前再次认证并提示 KDBX 体积影响。
- [ ] B08 历史支持差异查看和恢复；旧敏感值保持遮挡，恢复本身生成新历史。
- [ ] B09 多选支持移动、复制、归档、删除和添加标签；部分失败展示成功数和失败项。
- [ ] B10 归档不进入普通列表与默认搜索；可恢复到原分组或指定分组。
- [ ] B10 删除进入 KDBX 回收站；永久删除和清空必须再次认证并二次确认。

### 7.5 保险库模块状态

- [ ] 覆盖加载、空库、空分组、无搜索结果、只读、离线、同步中、冲突、文件移动、损坏和空间不足。
- [ ] Phone 使用独立详情路由；返回后恢复列表位置、筛选和选中项。
- [ ] 所有长列表、表单和敏感字段使用稳定内容面，悬浮控件不遮挡正文和键盘。

退出条件：关闭网络后仍可创建/打开库、浏览、搜索、管理六类条目、附件、历史、归档和回收站；破坏性操作均有确认与恢复路径。

## 8. 阶段 4：安全中心模块

目标：完成底栏“安全中心”，所有分析基于已解锁模型，默认不联网。

### 8.1 C01 安全概览

- [ ] 展示 0-100 分、问题数、上次检测时间和检测范围。
- [ ] 分类覆盖泄露、弱密码、重复密码、旧密码、过期项目、重复条目、不安全 HTTP 和缺少 TOTP。
- [ ] 严重问题优先；每类显示数量、严重度和查看入口。
- [ ] 检测时保留旧结果并显示增量进度；失败不得清空旧结果。
- [ ] 泄露检测关闭时显示“未检测”，不得暗示安全。

### 8.2 C02-C03 风险列表与修复

- [ ] 风险列表展示条目标题、问题摘要、严重度和修改时间，支持严重度、分组和类型筛选。
- [ ] 重复密码按同一秘密分组但不显示秘密；泄露结果不显示哈希前缀。
- [ ] 风险详情展示证据摘要、风险解释、受影响条目和明确修复入口。
- [ ] 弱、重复、旧密码进入生成器；HTTP、过期项目、重复条目和缺少 TOTP 进入对应修复流程。
- [ ] 忽略风险必须记录原因，并可在设置中恢复。

### 8.3 C04 泄露检测

- [ ] 在线泄露检测默认关闭，首次启用前展示隐私说明。
- [ ] 哈希在本地计算，只发送哈希前缀，不发送密码、完整哈希、用户名、网址或条目元数据。
- [ ] 关闭功能时删除本地在线查询缓存。
- [ ] 离线时只执行本地检测，并明确未执行的在线分类。

### 8.4 安全中心状态

- [ ] 覆盖从未检测、检测中、无风险、存在风险、在线检测关闭、离线和检测失败。
- [ ] 风险正文保持稳定内容面；范围、筛选和重新检测使用可收拢操作层。
- [ ] 检测结果准确，不在日志、路由或诊断信息中泄露秘密与哈希信息。

退出条件：本地检测结果准确且可完成修复闭环；在线检测只能由用户明确启用，并满足最小披露原则。

## 9. 阶段 5：生成器模块

目标：完成底栏“生成器”，并向保险库编辑和安全中心修复提供同一生成能力。

### 9.1 C05 密码生成器

- [ ] 随机密码默认 20 位，支持长度、大小写、数字、符号、相似字符排除和最少数量。
- [ ] 易记密码默认 4 个词，支持词数、分隔符、大小写和附加数字。
- [ ] PIN 默认 6 位，支持 4-12 位和避开连续/重复模式。
- [ ] 实时显示强度与估算，不使用“绝对安全”措辞。
- [ ] 支持重新生成、复制、保存为登录条目和填入当前编辑字段。
- [ ] 非法规则组合明确提示，不静默修改用户规则。
- [ ] 独立生成器离开、切后台或锁库时清除未保存候选。

### 9.2 C06 TOTP

- [ ] 支持二维码扫描、手动密钥和 `otpauth` URI。
- [ ] 相机权限拒绝后保留手动输入；不循环请求权限。
- [ ] 支持发行方、账户、密钥、位数、周期和算法，未知 URI 参数原样保留。
- [ ] 保存前生成预览验证码；密钥始终遮挡，显示前再次认证。
- [ ] 详情页显示稳定尺寸验证码、倒计时环和文本剩余秒数。

### 9.3 C07 安全复制与放大显示

- [ ] 复制后显示条件清除倒计时；仅当剪贴板仍为本应用写入内容时清除。
- [ ] 用户复制其他内容后停止本次任务，不覆盖新内容。
- [ ] 支持 15、30、60 秒；“永不”需要风险确认，默认 30 秒。
- [ ] 放大显示前再次认证，使用高对比度和分段字符，10 秒后关闭。
- [ ] 复制提示、通知和放大入口不回显完整秘密。

退出条件：三种生成模式、TOTP 和安全复制独立可用，并能从条目编辑、安全修复进入后正确返回原上下文。

## 10. 阶段 6：设置模块

目标：完成底栏“设置”，集中承载安全、保险库数据、WebDAV 同步、外观、隐私和恢复能力。

### 10.1 D00 设置首页

- [ ] Phone 使用单列分区：安全、保险库与数据、同步、审计、外观与无障碍、自动填充实验、关于与隐私。
- [ ] 每项显示有效摘要，如自动锁定时长、WebDAV 状态和泄露检测状态。
- [ ] 自动填充标记“实验性”；不出现账号、订阅、家庭或团队入口。
- [ ] 锁定时统一进入安全遮罩和解锁流程。

### 10.2 D04-D06 本地设置

- [ ] D04 自动锁定支持立即、1、5、15、30 分钟，默认 5 分钟。
- [ ] D04 生物快速解锁关闭需主密码确认；截图与最近任务保护默认开启。
- [ ] D04 密码显示默认 10 秒，剪贴板清除默认 30 秒，高风险再认证不可关闭。
- [ ] D05 展示文件名、格式、来源、大小、条目数、修改时间和只读状态。
- [ ] D05 修改主密码或密钥文件前验证当前复合凭据并自动备份。
- [ ] D05 支持手动备份、导出副本和重新定位；未加密导出必须再次认证和风险确认。
- [ ] D06 支持泄露检测缓存与忽略项、跟随系统/浅色/深色、高对比度和无障碍设置。
- [ ] D06 光感强度跟随系统，不创建冲突的私有强度档位。

### 10.3 A05、D01-D03 WebDAV 与同步

- [ ] A05 配置服务器、用户名、密码和远端路径；密码默认遮挡且编辑时不回显旧值。
- [ ] A05 测试连接并分类 DNS、超时、认证、证书、404 和只读错误。
- [ ] TLS 证书异常只允许取消或重新检查，不提供默认忽略。
- [ ] D01 展示来源、最后同步、本地修改、远端状态和网络状态。
- [ ] D01 支持手动同步、暂停自动同步和活动记录；离线修改进入本地队列。
- [ ] 同步状态覆盖空闲、同步中、已同步、离线、需要认证、错误和冲突。
- [ ] D02 本地与远端同时变化时暂停同步，不默认选择破坏性操作。
- [ ] D02 支持保留本地、使用远端和另存两份；替换前备份，失败保留双方文件。
- [ ] D03 支持编辑凭据、测试连接、立即同步、断开同步和清除活动记录。
- [ ] 活动记录只保存时间、动作、方向、结果和脱敏错误。

### 10.4 D08-D09 恢复、关于与隐私

- [ ] D08 覆盖文件不存在、损坏、空间不足、WebDAV 认证失效、证书错误、无网络和权限失效。
- [ ] 每个错误提供一个推荐恢复动作和一个安全次要动作，不形成无限重试。
- [ ] D09 展示版本、KDBX 兼容范围、开源许可、隐私说明和脱敏诊断导出。
- [ ] 隐私说明区分 WebDAV、泄露检测和系统剪贴板三类外部数据流。
- [ ] 诊断导出默认移除路径、用户名、网址、条目名和敏感值，并要求再次认证。

退出条件：设置摘要均反映真实状态；WebDAV 离线编辑可恢复同步；冲突绝不静默覆盖；所有安全设置即时作用于全局。

## 11. 阶段 7：Tablet、状态覆盖与 P0 发布收敛

### 11.1 响应式布局

- [ ] Phone 竖屏使用单栏，条目详情进入独立页面。
- [ ] Phone 横屏保持单栏或紧凑双栏，表单字段不压缩到不可读。
- [ ] Tablet 保险库使用“导航 + 条目列表 + 详情”三栏布局。
- [ ] Tablet 安全中心、生成器和设置使用“导航 + 内容”双栏布局。
- [ ] Tablet 未选择条目时显示中性占位，不自动展示敏感内容。
- [ ] 复用 `features/adaptiveLayout` 与 `features/responsiveLayout`，移除演示 UI。

### 11.2 全局状态矩阵

| 状态     | 必须覆盖范围           | 验收要求        |
|--------|------------------|-------------|
| 空数据    | 保险库、分组、搜索、归档、回收站 | 提供一个明确下一步   |
| 加载     | 启动、打开、搜索、审计、同步   | 不展示旧敏感内容    |
| 锁定     | 全局               | 只展示库摘要和认证入口 |
| 只读     | 保险库、详情、编辑、同步     | 禁用写操作并说明原因  |
| 离线     | 保险库、安全中心、同步      | 本地可用并标记待同步  |
| 权限拒绝   | 文件、相机、生物认证       | 解释用途，避免循环请求 |
| 错误凭据   | 解锁、WebDAV        | 不泄露具体失败字段   |
| 文件损坏   | 打开、同步            | 保留原文件并提供恢复  |
| 同步冲突   | 顶栏、同步中心、冲突面板     | 暂停同步且不默认覆盖  |
| 在线检测关闭 | 安全中心、设置          | 显示“未检测”     |

### 11.3 敏感操作契约

| 操作         | 默认行为       | 再认证        |
|------------|------------|------------|
| 查看密码       | 遮挡，显示 10 秒 | 库已解锁时不额外要求 |
| 复制密码/TOTP  | 30 秒后条件清除  | 库已解锁时不额外要求 |
| 放大显示密码     | 10 秒后关闭    | 必须         |
| 导出附件/未加密数据 | 风险确认       | 必须         |
| 修改主密码/密钥文件 | 先备份        | 当前复合凭据     |
| 永久删除/清空回收站 | 二次确认       | 必须         |
| 开启生物解锁     | 系统认证       | 必须         |

### 11.4 发布收敛

- [ ] 浅色、深色、高对比度下的文字、图标和风险状态清晰可读。
- [ ] 大字体、长网址、长文件名和按钮文案无重叠或横向溢出。
- [ ] 所有页面检查后台、截图、最近任务、系统返回和路由栈行为。
- [ ] 所有错误信息可行动且不泄露敏感数据。
- [ ] 补齐正式 vendor、版本、签名配置、图标和发布资源。
- [ ] Phone `390 x 844` 与 Tablet `1280 x 800` 完成关键流程人工验收。

退出条件：全部 P0 项实现并人工验收；Phone 与 Tablet 信息架构一致，布局、无障碍和安全状态完整。

## 12. 阶段 8：P1 自动填充能力验证

- [ ] D07 调研并验证 API 26 系统级自动填充所需组件、权限和系统入口。
- [ ] D07 单独验证 ArkWeb 浏览器扩展路线，不与系统级能力混用结论。
- [ ] D07 记录可用、需配置、不支持、组件缺失、测试成功和测试失败状态。
- [ ] D07 能力可用时再实现实验入口与测试字段。
- [ ] D07 能力不可用时保留安全复制，不在首次引导展示自动填充。

退出条件：形成可复现的能力结论；P0 不依赖自动填充。

## 13. 核心数据契约

```text
VaultSource = LocalFile | WebDAV

VaultFormat =
  | KDBX4ReadWrite
  | KDBX31UpgradeRequired
  | Unsupported
  | Corrupt

VaultCredential = {
  masterPassword: required,
  keyFile: optional,
  biometricQuickUnlock: optional-after-first-successful-unlock
}

SyncState = idle | syncing | synced | offline | authRequired | error | conflict

ConflictResolution = KeepLocal | UseRemote | SaveBoth

ItemTemplate = Login | PaymentCard | SecureNote | Identity | WiFi | SoftwareLicense

AuditFinding = {
  type,
  severity,
  affectedItemIds,
  evidenceSummary,
  detectedAt,
  status: open | ignored | resolved,
  ignoreReason?: string
}
```

## 14. 人工验收流程

### 14.0 阶段 2 架构回归

- [ ] 冷启动时先显示安全遮罩，不闪现保险库、条目标题或密码内容。
- [ ] 创建新库与打开已有库两条入口均可完成解锁，阶段 1 已验收数据仍可正常读取和保存。
- [ ] 底部“保险库 / 安全中心 / 生成器 / 设置”均通过真实路由进入；各模块首页首次返回提示“再次返回则回到桌面”，2
  秒内再次返回仅将应用切至后台，不销毁应用且不显示 Navigation 占位内容。
- [ ] 解锁页首次返回提示“再次返回则回到桌面”，2 秒内再次返回仅将应用切至后台，不跳转至其他页面。
- [ ] 各模块首页等待超过 2 秒后再次返回仍只显示提示；切换 Tab 后不会沿用另一 Tab 的二次返回计时；当前 Tab
  路由栈异常为空时再次点击可恢复首页。
- [ ] 安全中心与设置只显示明确的未开发空状态，没有伪功能或无效操作。
- [ ] 独立生成器路由可修改三种模式的全部规则、立即刷新并复制候选，不显示“保存为新登录条目”。
- [ ] 新增或编辑条目时点击“生成”打开半模态生成器；规则与独立生成器一致，“填入当前条目”关闭半模态并回填密码。
- [ ] 保险库搜索、收藏/归档筛选、分组筛选、新增条目和编辑条目分组均与阶段 1 行为一致。
- [ ] 在密码已显示或生成器已有候选时切到后台，系统任务界面不泄露内容；回前台时秘密恢复为隐藏状态。
- [ ] 从四个底栏模块分别手动锁定，均回到解锁页；再次解锁后进入保险库，旧搜索词、生成器候选和编辑密码已清除。
- [ ] 复制密码后显示 30 秒倒计时；倒计时内手动复制其他文本，应用不清除后来复制的文本。
- [ ] 连续输入错误主密码时重复提交延迟仍生效，正确密码可在延迟结束后解锁。
- [ ] 在 Phone 浅色、深色和系统大字体下检查顶部图标、内容、底部胶囊无重叠、横向溢出或不可读截断。
- [ ] 底部胶囊悬浮覆盖在页面内容层之上，页面内容可滚动到胶囊上方；胶囊下方系统安全区与页面背景同色。

### 14.1 新建本地库

- [ ] 从首次使用页创建库，设置主密码和可选密钥文件。
- [ ] 创建成功后可启用生物解锁并进入空保险库。
- [ ] 新建登录条目、生成密码、保存、锁定并再次解锁读取。

### 14.2 打开现有库

- [ ] 选择 KDBX 4.x，使用主密码和密钥文件解锁。
- [ ] 错误密码、缺少密钥文件和只读文件均有独立恢复路径。
- [ ] KDBX 3.1 先备份再升级；备份失败不修改原文件。

### 14.3 保险库完整流程

- [ ] 分组、搜索、六类条目、附件、历史、多选、归档和回收站完整可用。
- [ ] 密码显示 10 秒后隐藏，切后台立即隐藏。
- [ ] 复制后显示倒计时；用户复制新内容后不清除新内容。
- [ ] 放大显示、导出、修改凭据和永久删除触发再次认证。

### 14.4 安全中心与生成器

- [ ] 本地检测发现弱、重复、旧密码和不安全 HTTP 地址。
- [ ] 在线检测未启用时显示未检测；启用后仅发送哈希前缀。
- [ ] 三种生成模式可独立使用并回填条目。
- [ ] 可扫描或手动添加 TOTP，详情显示验证码和倒计时并可安全复制。

### 14.5 WebDAV、离线与冲突

- [ ] 测试连接后打开远端库，完成一次上传和下载。
- [ ] 离线编辑显示待同步；恢复网络后先比较版本再同步。
- [ ] 认证失效、证书异常和文件不存在提供明确恢复入口。
- [ ] 本地与远端同时修改时进入冲突，三种解决方式均先保护原版本。

### 14.6 响应式与无障碍

- [ ] Phone 无横向溢出、按钮遮挡和不可读截断。
- [ ] Tablet 同时展示符合模块要求的导航、列表和详情区域。
- [ ] 浅色、深色、高对比度和大字体状态均可用。
- [ ] 风险不只依赖颜色，减少透明度和减少动态效果不改变功能。

## 15. 阶段验收规则

每个阶段按以下顺序收敛：

1. 完成该阶段纵向调用链和全部列明状态。
2. 执行项目静态检查，确认 ArkTS、性能和安全规则。
3. 使用 `devecocli build --product default --modules default@default --build-mode debug` 验证编译、资源和配置。
4. 不执行单元测试、`ohosTest`、白盒测试或自动化运行测试。
5. 开发代理不启动模拟器、不安装或运行应用。
6. 用户按阶段退出条件手动验证功能、交互、视觉和设备适配。
7. 修复人工验收问题后，才能勾选任务并进入下一阶段。

构建成功只代表代码、资源和配置可编译，不代表 KDBX 兼容性、密码学行为或业务流程正确。

## 16. 里程碑

| 里程碑        | 交付物                  | 依赖 | 状态  |
|------------|----------------------|----|-----|
| M0 技术闸门    | KDBX、安全写入、系统能力结论     | 无  | [x] |
| M1 本地最小闭环  | 创建、打开、解锁、登录条目、保存、锁定  | M0 | [x] |
| M2 架构与全局底座 | V2 状态、模块边界、全局安全与错误模型 | M1 | [ ] |
| M3 保险库     | 完整本地条目、搜索、附件、历史、归档   | M2 | [ ] |
| M4 安全中心    | 本地审计、泄露检测和修复闭环       | M3 | [ ] |
| M5 生成器     | 三种生成模式、TOTP、安全复制     | M4 | [ ] |
| M6 设置      | 本地设置、WebDAV、同步、冲突、隐私 | M5 | [ ] |
| M7 P0 发布候选 | Tablet、无障碍、状态覆盖、人工验收 | M6 | [ ] |
| M8 P1 验证   | 自动填充与 ArkWeb 能力结论    | M7 | [ ] |

## 17. 当前下一步

- [x] 完成阶段 2 的状态管理 V2 架构设计，明确 AppSession 与已有业务模块的状态所有权。
- [x] 将巨型入口页面拆分为 AppShell、独立 Page 和 ViewModel，不改变已验收业务行为。
- [x] 把锁定、前后台、再次认证、错误分类和敏感状态清理收敛为全局能力。
- [x] 使用 `devecocli` 完成 API 26 Debug 构建验证。
- [ ] 用户完成 14.0 阶段 2 架构回归并反馈结果。
- [ ] 阶段 2 人工验收通过后勾选 M2，再进入阶段 3 保险库模块。

## 18. 风险与决策记录

| 日期         | 事项                     | 状态  | 决策                                    |
|------------|------------------------|-----|---------------------------------------|
| 2026-08-19 | KDBX 实现方案              | 已关闭 | 使用 `kdbxweb@2.3.0`，已通过 API 26 人工验证    |
| 2026-08-19 | 系统备份可能越过密码数据边界         | 已关闭 | 关闭文件与结构化数据云同步                         |
| 2026-08-19 | 不执行白盒和自动化运行测试          | 已确认 | 保留静态检查与构建，功能由用户手动验收                   |
| 2026-08-20 | 两份计划导致需求与进度分离          | 已关闭 | 合并为本统一计划，页面要求和开发顺序同处维护                |
| 2026-08-20 | 后续页面继续使用 V1 会扩大迁移成本    | 已确认 | 全局底座先迁移 V2；V1 仅限有官方依据的局部例外            |
| 2026-08-20 | 未开发模块是否提前创建 ViewModel  | 已关闭 | 不创建无状态空 ViewModel；首个真实业务状态出现时建立 V2 边界 |
| 2026-08-20 | 当前是否接入 `PersistenceV2` | 已关闭 | 尚无用户设置，不创建无主持久化状态；敏感值始终禁止接入           |
| 2026-08-20 | 沉浸材质低算力降级方式            | 已关闭 | 使用系统材质按设备算力自动分档，不维护手写视觉回退分支           |
| 2026-08-23 | 通用工具能力是否自研              | 已关闭 | 统一接入 `@pura/harmony-utils@1.4.2`（LogUtil/PreferencesUtil/FileUtil/ToastUtil/StrUtil/RandomUtil），删除等价自研；安全语义（密码 CSPRNG 随机索引、剪贴板所有权清除、asset/userAuth 流程）保留自研，详见 AGENTS.md 技术栈规范 |

## 19. 参考基线

- [HarmonyOS 沉浸光感设计指南](https://developer.huawei.com/consumer/cn/doc/design-guides/immersivelight-0000002612101053)
- [HarmonyOS 色彩设计指南](https://developer.huawei.com/consumer/cn/doc/design-guides/color-0000001776857164)
- HarmonyOS 本地开发文档：状态管理 V2、MVVM V2、Navigation、Immersive Material、HDS Navigation、HDS Tabs、响应式布局、User
  Authentication Kit、Pasteboard、隐私窗口与文件服务。
- [1Password for Android](https://support.1password.com/getting-started-android/)
- [1Password Watchtower](https://support.1password.com/watchtower/)
- [1Password password generator](https://support.1password.com/generate-website-password/)

参考资料只用于核对能力覆盖和交互完整性；产品信息架构、文案、品牌和视觉保持原创。
